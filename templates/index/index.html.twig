{% extends 'base.html.twig' %}

{% block title %}Hello IndexController!{% endblock %}

{% block body %}
    <div class="block-nav" >
        <h1> Christian COLEY</h1>
        <h2 class="status"> Développeur Web & Mobile </h2>
        <img class="photoChristian" id="bio" src="/pictures/chris.jpg" alt="photo de christian">
    </div>
    <section class="welcome">
        <div class ='bio'>
            <h2 class="textWelcome">Bienvenue dans mon site!!!</h2>
            <p class="textBio"> Actuellement à la WILD CODE SCHOOL de Lyon, je me forme afin d’acquérir un savoir-faire pour intégrer une équipe passionnée qui me permettra de développer mes compétences.

                Après une dizaine d’années en agroalimentaire, j’ai décidé de me donner une nouvelle orientation.  </p>
        </div>
    <div class = "buttonWelcome" >
        <button class="btn btn-warning" ><a href="/items/CV-Christian-COLEY.pdf" download="CV-Christian-COLEY.pdf"> Télécharger le CV</a></button>
        <button class="contact btn btn-primary"><a href="#contact">Me Contacter</a></button>
    </div>
    </section>

    <section class="mesProjets" id="mesprojets">
        <h2 class="sectionTitle">Mes Projets Web</h2>

        <article>
            <div class="container-fluid">
                <div class="row">

            {% for projects in project %}
            <div class="card-deck col-lg-4">
                <div class="card">
                    <a href="{{projects.linkUrl}}"><img class="card-img-top" src="{{ projects.screenShoot }}" alt="Card image cap"></a>
                    <div class="card-body">
                        <h5 class="card-title">{{ projects.name }}</h5>
                        <p class="card-text">{{ projects.comment }}</p>
                        <div class="container-fluid">
                            <div class="row">
                        {% for  technologie in projects.technologies %}
                            <div class="col-4">
                       <img style="height: 5rem; width: 100px" class=" img-fluid logo" src="{{ technologie.logo }}" alt="logo {{ technologie.name }}">
                            </div>
{#                            modifier les logos#}
                        {% endfor %}
                            </div>
                        </div>

                        <a href="{{projects.linkUrl}}"><p class="card-text"><small class="text-muted">{{projects.linkUrl}}</small></p></a>
                    </div>
                </div>
            </div>
            {% endfor %}
                </div>
            </div>
        </article>
    </section>

    <section class='sectionContact' id="contact" >
        <h2 class="sectionTitle"> Me contacter</h2>
        <div class="container">


            {{ form_start(form) }}
            {{ form_row(form.firstname,{'label': 'Votre prénom'   }) }}
            {{ form_row(form.lastname,{'label': 'Votre nom'   }) }}
            {{ form_row(form.email,{'label': 'Votre e-mail'   }) }}
            {{ form_row(form.subject,{'label': 'Sujet'   }) }}
            {{ form_row(form.text,{'label': 'Votre text'   }) }}
            <button class="btn btn-secondary " type="submit" name="submit"> Envoyer </button>
            {{ form_end(form) }}

        </div>
    </section>










{% endblock %}
